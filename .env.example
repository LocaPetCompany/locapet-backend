# =============================================================================
# Locapet Backend - Environment Variables
# AWS Secrets Manager에 동일한 키로 저장 (locapet/{env}/app)
# =============================================================================

# --- Database ---
DB_URL=jdbc:postgresql://localhost:5432/locapet
DB_USERNAME=root
DB_PASSWORD=password

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379

# --- Spring Profile ---
SPRING_PROFILES_ACTIVE=local

# --- JWT ---
JWT_SECRET=locapet-jwt-secret-key-must-be-at-least-32-characters-long

# --- Identity Verification (PASS) ---
CI_HMAC_SECRET=locapet-ci-hmac-secret-key-must-be-at-least-32-characters-long
IDENTITY_VERIFICATION_PROVIDER=mock

# --- Social Login ---
GOOGLE_CLIENT_ID=your-google-client-id
APPLE_CLIENT_ID=com.vivire.locapet

# --- Docker (deploy only) ---
ECR_REGISTRY=123456789012.dkr.ecr.ap-northeast-2.amazonaws.com
IMAGE_TAG=latest
